# دليل السكلات — مشروع حلقة التحفيظ

> برنامج يسجّل حفظ كل طالب، يطابقه بزميل في نفس المستوى، ويولّد لهم جدول مراجعة.

عندك 27 سكل مثبّت. هذا الملف يقول أي واحد تستدعي في أي مرحلة من **هذا المشروع تحديداً** — ولماذا الباقي لا.

---

## القاعدة الأولى: مشروعك تطبيق بيانات، مو صفحة هبوط

هذا أهم سطر في الملف. مشروعك = جداول + نماذج إدخال + منطق مطابقة + تقويم. أغلب السكلات اللي نزلناها مصمّمة لصفحات تسويقية وبورتفوليو، وهذي فئة مختلفة تماماً.

`design-taste-frontend` (أكبر سكل عندك، 88 KB) **يستثني مشروعك بنصّه الصريح** في أول سطر:

> Landing pages, portfolios, and redesigns. **Not dashboards, not data tables, not multi-step product UI.**

فلا تستدعيه لهذا المشروع. احفظه لو سويت لاحقاً صفحة تعريفية بالبرنامج.

---

## الترتيب العملي

### المرحلة 0 — قبل أي كود
| السكل | ليش |
|---|---|
| `anthropic-skills:business-analysis` | يحدد نطاق الـ MVP: وش أقل شي يشتغل؟ ويكشف القرارات المؤجّلة (وش يعني "نفس المراجعة" بالضبط؟ الجزء؟ الصفحة؟ السرعة؟) |

ابدأ من هنا. منطق المطابقة هو قلب المشروع، وأي غلط في تعريفه يهدم الباقي.

### المرحلة 1 — نظام التصميم
| السكل | الاستخدام |
|---|---|
| `ui-ux-pro-max` | يولّد ألوان + خطوط + قواعد UX. استعلم بـ `"quran memorization education dashboard arabic RTL"` — لا تكتب استعلاماً عاماً |
| `design-system` | يثبّت المخرجات كـ design tokens بدل ما تتكرر يدوياً |

⚠️ راجع مخرجات `ui-ux-pro-max` قبل ما تعتمدها. في اختبار سابق أعطاني نمط "App Store Landing" لمتجر عسل — أحياناً يخطئ في قراءة نوع المنتج.

### المرحلة 2 — الشكل العام (اختر **واحداً** فقط)
| السكل | متى |
|---|---|
| `minimalist-ui` | ✅ **المرشّح لمشروعك** — واجهات تحريرية نظيفة، بدون تدرجات ولا ظلال ثقيلة. مناسب لسياق تعليمي/قرآني محترم |
| `high-end-visual-design` | لو تبي إحساس "وكالة فخمة" — يبني على الظلال والتدرجات |
| `apple-design` | لو رايح على تطبيق جوال بحركة فيزيائية |

🚫 **لا تجمع بينها.** `minimalist-ui` يمنع بالضبط ما يبنيه `high-end-visual-design`. اختيار واحد ثم الالتزام به.

### المرحلة 3 — المكوّنات (أكثر مرحلة ستقضي فيها وقتاً)
| السكل | الاستخدام |
|---|---|
| `ui-styling` | shadcn/ui — جداول، نماذج، dialogs، dark mode. **هذا عمودك الفقري**: كل شاشات المشروع جداول ونماذج |
| `pick-ui-library` | لاختيار مكتبة التقويم / الجدول / الـ drag-and-drop. استدعِه بالاسم — لا يتفعّل تلقائياً |

### المرحلة 4 — الحركة (بعد ما يشتغل كل شي)
| السكل | الاستخدام |
|---|---|
| `emil-design-eng` | فلسفة الصقل والتفاصيل الخفية |
| `find-animation-opportunities` | يمسح الكود ويقترح وين تنفع حركة |
| `review-animations` | مراجعة صارمة لكودك. استدعِه بالاسم |
| `animation-vocabulary` | لما تبي تصف حركة ولا تعرف اسمها |

لا تبدأ من هنا. الحركة تجي بعد أن يعمل المنطق.

### المرحلة 5 — الهوية (اختياري)
`brand` · `brandkit` · `banner-design` · `slides` — لو حبيت اسم وشعار وعرض تقديمي للحلقة.

---

## سكلات تجاهلها في هذا المشروع

| السكل | السبب |
|---|---|
| `design-taste-frontend` + `-v1` | يستثني تطبيقات البيانات بنصّه |
| `industrial-brutalist-ui` | جمالية عسكرية / "ملفات مسرّبة" — لا تناسب سياق قرآني |
| `gpt-taste` | حركات GSAP ثقيلة لصفحات awwwards، تعيق تطبيقاً داخلياً |
| `stitch-design-taste` | مخصص لأداة Google Stitch |
| `imagegen-frontend-web` / `-mobile` / `image-to-code` | توليد صور تصميم — تحتاج مفتاح API، ومرحلة متأخرة |
| `redesign-existing-projects` | لتحسين مشروع قائم، ومشروعك من الصفر |
| `full-output-enforcement` | يجبر مخرجات ضخمة بلا اختصار — مفيد لملف واحد طويل، لكنه يضخّم كل رد |

---

## كيف تستدعي

لا تعتمد على التفعيل التلقائي — 27 سكل تتزاحم على نفس الكلمات المفتاحية. سمِّ السكل صراحة:

> استخدم `ui-styling` لبناء جدول الطلاب مع فرز حسب الجزء

> استخدم `pick-ui-library` لاختيار مكتبة تقويم تدعم العربية و RTL

---

## متطلبات تقنية مثبّتة

- **Python 3.13.14** — `C:\Users\Aisha\AppData\Local\Programs\Python\Python313\python.exe` (يشتغل بأمر `python` بعد إعادة تشغيل Claude Code)
- **Node v24.16** — لـ `npx shadcn`
- **ناقص:** مفتاح Gemini + `pip install google-genai pillow` — فقط لو احتجت توليد لوجو عبر سكل `design`
