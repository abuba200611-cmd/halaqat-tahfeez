# تصميم النواة — برنامج حلقة التحفيظ

> **منتج تجاري.** المشتري = إدارة حلقة تدير كل شيء يدوياً. القيمة المباعة = إلغاء العمل اليدوي في التسجيل والربط.

## القرارات المعتمدة

| القرار | الاختيار |
|---|---|
| المنصة | ويب + جوال → **PWA واحد** |
| معيار المطابقة | تقاطع المحفوظ + مستوى الإتقان |
| عدد الطلاب | **١٥٠+** |
| إدخال البيانات | **الطالب يسجّل وشريكه يعتمد** |
| نموذج البيع | هذه الحلقة أولاً، ثم التعميم |

---

## خطة الإطلاق على مرحلتين

الترتيب هنا ليس تقنياً بل تجارياً — كل مرحلة تحل مشكلة مختلفة.

### المرحلة ١ — محرك المطابقة (هذي اللي تبيعها)
مستخدم واحد: **المعلّم**. لا حسابات طلاب، لا تسجيل دخول جماعي.

- استيراد قائمة الطلاب ومحفوظ كل واحد
- توليد الثنائيات آلياً
- المعلّم يراجع ويعدّل ويعتمد
- طباعة/تصدير الجدول

**لماذا أولاً:** مستخدم واحد تُدرّبه في ربع ساعة، وقيمة ظاهرة من أول يوم — تختصر عليه ساعات الربط اليدوي لـ ١٥٠ طالباً. وهذا وحده يكفي لإتمام البيع أو بدء تجربة.

### المرحلة ٢ — التسجيل الذاتي (هذي اللي تُبقيهم)
١٥٠ حساب طالب. تغذّي درجات الإتقان، فتتحسّن المطابقة تلقائياً.

**لماذا ثانياً:** تشغيل ١٥٠ حساباً هو أصعب جزء في المشروع. لا تبدأ به وأنت لم تُثبت القيمة بعد.

---

## خطران يقتلان المشروع إن أُهملا

### ١. تسجيل الدخول لـ ١٥٠ طالباً
بريد وكلمة مرور = فشل مؤكد، خاصة لو الطلاب صغار. سينسون، وستقضي وقتك في إعادة التعيين لا في التطوير.

البدائل مرتبة بالأفضلية:
- **رابط سحري / رمز QR** يوزّعه المعلّم مرة واحدة، والجهاز يتذكّر الطالب بعدها
- **رمز قصير لكل طالب** (٦ خانات) بلا كلمة مرور
- رمز OTP على الجوال — يكلف مالاً، وكثير من الطلاب بلا جوال

اعتماد الشريك يجب أن يكون **ضغطة واحدة**. أي نموذج إدخال هنا يقتل الالتزام.

### ٢. إدخال بيانات ١٥٠ طالباً أول مرة
هذه ليست مشكلة برمجية بل تشغيلية، وهي **أكبر عائق أمام الإطلاق**. لن يجلس المعلّم يكتب محفوظ ١٥٠ طالباً يدوياً.

الحل يُخطَّط له من البداية:
- استيراد من ملف Excel/Sheets إن كانت سجلاتهم رقمية
- أو: الأسبوع الأول = كل طالب يُدخل محفوظه بنفسه، والمشرف يعتمد
- أو: البدء بمجموعة واحدة (٢٠ طالباً) كتجربة، ثم التوسّع

---

## 1. المنصة: تطبيق ويب واحد (PWA) — لا تطبيقان

طلبت جوال **و** ويب. لا تبنِ اثنين. ابنِ تطبيق ويب متجاوب ثم فعّل PWA:

- يُثبَّت على شاشة الجوال بأيقونة، ويفتح بملء الشاشة بلا شريط متصفح — الطالب يحسّه تطبيقاً
- كود واحد، نشر واحد، تحديث فوري بلا انتظار مراجعة المتاجر
- لا حساب مطوّر (٩٩ دولاراً سنوياً لآبل، ٢٥ لجوجل)

**التكلفة الحقيقية:** إشعارات الدفع على iOS تحتاج iOS 16.4+ وتثبيت التطبيق يدوياً. لو التذكير بموعد التسميع ركن أساسي عندك، هذا القيد يستحق نقاشاً قبل البدء.

**الحزمة:** Next.js + Tailwind + shadcn/ui + Supabase (قاعدة بيانات + مصادقة). تدعم RTL، وتوافق سكل `ui-styling` المثبّت عندك.

---

## 2. مشكلة في معيار المطابقة

اخترت "الموضع + الإتقان". هذا **ناقص**، وهذي أهم ملاحظة في الملف:

الموضع الحالي لا يكفي للتسميع المتبادل. تخيّل طالبين كلاهما "عند الجزء ٥":
- **الأول** حفظ من جزء عمّ نازلاً: ٣٠ ← ٢٦
- **الثاني** حفظ من البقرة صاعداً: ١ ← ٥

كلاهما "عند الجزء ٥" بعدد الأجزاء، لكن **ما بينهما صفحة مشتركة واحدة**. لا أحد منهما يقدر يسمّع للآخر.

المعيار الصحيح: **تقاطع المحفوظ** (intersection)، لا تطابق الموضع.

```
صالحان للمطابقة  ⟺  |محفوظ(أ) ∩ محفوظ(ب)| كبير كفاية
                  و  |إتقان(أ) − إتقان(ب)| صغير كفاية
```

لذلك النموذج يخزّن **نطاقات** لكل طالب، لا رقماً واحداً — لأن الحفظ غالباً متقطّع (جزء عمّ + البقرة + الكهف).

---

## 3. نموذج البيانات

```
Student          id, name, gender, phone, halaqa_id, active

MemorizedRange   student_id, from_page, to_page, memorized_at
                 ← عدة صفوف لكل طالب (الحفظ متقطّع)

Session          student_id, listener_id, date,
                 from_page, to_page,
                 errors, hesitations, rating(1-5)
                 ← كل تسميع يُسجَّل، ومنه يُحسب الإتقان

Pairing          week_start, student_a, student_b,
                 overlap_from, overlap_to

ScheduleItem     pairing_id, date, from_page, to_page,
                 kind: جديد | مراجعة_قريبة | مراجعة_بعيدة
```

الصفحة (١–٦٠٤) هي وحدة القياس، لا الجزء — أدق بـ ٢٠ ضعفاً وتسهّل الحساب.

**الإتقان** ليس حقلاً يُدخَل يدوياً، بل يُحسب:

```
إتقان(طالب، نطاق) = متوسط مرجّح لتقييمات آخر جلسات غطّت النطاق
                      يتناقص كلما طال الزمن منذ آخر مراجعة
```

---

## 4. خوارزمية المطابقة

```
لكل زوج (أ، ب):
    التقاطع = صفحات(أ) ∩ صفحات(ب)
    إذا |التقاطع| < ٢٠ صفحة              → استبعد
    إذا |إتقان(أ) − إتقان(ب)| > الحد      → استبعد
    النقاط = |التقاطع|×و١ − فجوة_الإتقان×و٢ − تكرار_الشريك×و٣

رتّب الأزواج تنازلياً بالنقاط
خذ الأعلى فالأعلى، بشرط أن يكون الطرفان غير مرتبطَين

الفردي المتبقي → ثلاثي، أو مع المعلم
```

**قيود لا تنسها:**
- فصل الجنسين إن كانت الحلقة مختلطة الإدارة
- عدم تكرار نفس الشريك أسابيع متتالية (`تكرار_الشريك`)
- الغياب: إعادة مطابقة سريعة لا انهيار الجدول

لحلقة من ٢٠–٤٠ طالباً، الخوارزمية الجشعة (greedy) كافية تماماً. لا تستعمل خوارزميات المطابقة المثلى — تعقيد بلا فائدة على هذا الحجم.

---

## 5. الجدول

لكل ثنائي: توزيع التقاطع على أيام الأسبوع، بمبدأ التكرار المتباعد (spaced repetition):

- المحفوظ حديثاً → مراجعة أكثر تكراراً
- المتقن القديم → دورة أطول
- كل جلسة تُسجَّل، فتُحدَّث درجة الإتقان، فتتغيّر مطابقة الأسبوع القادم

النظام يتغذّى على نفسه: تسميع ← تقييم ← إتقان ← مطابقة ← جدول.

---

## حالة التنفيذ — ٢٤ يوليو ٢٠٢٦

المرحلة ١ (محرك المطابقة) قائمة وتعمل في `web/`:

| ما أُنجز | أين |
|---|---|
| مطابقة على تقاطع المحفوظ + فارق الإتقان | `lib/pairing.ts` |
| **جدول شهري** بتدوير الشركاء وتناوب عادل على التسميع للمعلّم | `lib/schedule.ts` |
| حدود الأجزاء الفعلية (بدل التوزيع المتساوي التقريبي) | `lib/quran.ts` |
| تخزين دائم + حساب معلّم | `lib/db.ts`، `lib/auth.ts`، `app/api/` |

قياس على ١٤١ طالباً: تغطية ٩٩٪، وجدول شهر كامل (٢٢ لقاء) في ~٢٥٠ م.ث،
بأكثر تكرار لثنائية واحدة = ٤ مرات من ٢٢.

### انحراف مقصود عن القرارات أعلاه

الجدول يذكر Supabase، والمنفَّذ فعلياً **SQLite محلي** خلف Route Handlers.
السبب عملي لا تقني: Supabase يتطلب حساباً سحابياً ومفاتيح، وهي خطوة يملكها
صاحب المشروع وحده. طبقة الوصول محصورة في `lib/db.ts` وحده، فالانتقال
لقاعدة سحابية عند الاستعداد للبيع يمسّ ملفاً واحداً.

### الخطوة التالية

بالترتيب:

1. **حفظ الجداول المولّدة** — الآن تُولَّد ولا تُحفظ، فلا سجل لما جرى في شهر مضى.
2. **تسجيل نتيجة كل جلسة تسميع** — هي التي تُحدّث درجة الإتقان، وبدونها
   تبقى المطابقة تعمل على تقدير أولي لا على واقع.
3. **الاستيراد من Excel** — أكبر عائق تشغيلي أمام الإطلاق (القسم ٢ من الخطرين).
